# Ignore compiled binaries everywhere
a.out
**/a.out
**/a.exe

# Ignore all hidden files/folders
.*

# Ignore everything by default
*

# But allow directories to be traversed (needed for nested folders)
!*/

# Then explicitly allow only the file types you want
!*.c
!*.csv
!*.pdf

# Always allow .gitignore itself
!.gitignore